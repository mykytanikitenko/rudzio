[package]
name = "no_preserve_originals_flag"
version = "0.1.0"
edition = "2024"
autotests = false

[dependencies]

[lib]
harness = false

[dev-dependencies]
rudzio = { version = "0.1", features = ["common", "runtime-tokio"] }
anyhow = "1.0"
