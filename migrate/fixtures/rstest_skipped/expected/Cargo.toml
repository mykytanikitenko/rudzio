[package]
name = "rstest_skipped"
version = "0.1.0"
edition = "2024"
autotests = false

[dependencies]
rudzio = { version = "0.1", features = ["common", "runtime-tokio"] }
anyhow = "1.0"

[dev-dependencies]
rstest = "0.22"
