[package]
name = "test_context_unresolvable"
version = "0.1.0"
edition = "2024"
autotests = false

[dependencies]
test-context = "0.3"

[dev-dependencies]
rudzio = { version = "0.1", features = ["common", "runtime-tokio"] }
anyhow = "1.0"
