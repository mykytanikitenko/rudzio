[package]
name = "cfg_lints_preserve_existing"
version = "0.1.0"
edition = "2024"

[dependencies]

[lints.rust]
unsafe_code = "deny"
unexpected_cfgs = { level = "warn", check-cfg = ["cfg(tokio_unstable)"] }
