[package]
name = "tokio_current_thread"
version = "0.1.0"
edition = "2024"
autotests = false

[dependencies]
tokio = { version = "1", features = ["full"] }

[lib]
harness = false

[dev-dependencies]
rudzio = { version = "0.1", features = ["common", "runtime-tokio-current-thread"] }
anyhow = "1.0"
