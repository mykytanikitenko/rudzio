[package]
name = "ambassador_verbatim"
version = "0.1.0"
edition = "2024"
autotests = false

[dependencies]

[lib]
harness = false
test = false

[dev-dependencies]
rudzio = { version = "0.1", features = ["common", "runtime-tokio-multi-thread"] }
anyhow = "1.0"
